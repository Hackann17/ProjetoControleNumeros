
class numeros:
        def __init__(self, operador, numero, ultima_recarga, proxima_recarga, status):
            self.operador = operador
            self.numero = numero
            self.ultima_recarga = ultima_recarga
            self.proxima_recarga = proxima_recarga
            self.status = status